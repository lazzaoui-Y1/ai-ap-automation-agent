# مراجعة أمنية أولية للمستودع

> تاريخ المراجعة: 2026-02-23
> النطاق: `backend/app` (FastAPI + Pipeline OCR/LLM)

## ملخص تنفيذي

تم اكتشاف عدة مخاطر عالية التأثير، أهمها:
1. **غياب المصادقة/التفويض** على جميع واجهات API الحساسة.
2. **Path Traversal** محتمل عبر `customer_id` واسم الملف المرفوع.
3. **تهيئة CORS غير آمنة** (`*` مع `allow_credentials=True`).
4. **تسريب معلومات داخلية** عبر إرجاع رسائل الاستثناء الخام للعميل.
5. **غياب حدود رفع الملفات** (الحجم/العدد) مما يفتح الباب لـ DoS.

---

## النتائج التفصيلية

### 1) لا توجد مصادقة أو تفويض على الـ API (High)
- **الوصف:** نقاط النهاية التي تعالج ملفات وتستعرض بيانات العملاء متاحة بدون أي آلية auth (JWT/API key/session).
- **الأثر:** أي طرف يمكنه رفع ملفات، استعراض العملاء، وسحب معلومات تشغيلية.
- **مؤشرات:** لا يوجد `Depends(get_current_user)` أو middleware أمني، والنقاط معرفة مباشرة.
- **توصية:** إضافة آلية مصادقة (JWT أو API key) + تفويض حسب `customer_id` + rate limiting.

### 2) Path Traversal عبر `customer_id` (High)
- **الوصف:** يتم بناء المسارات باستخدام f-string من إدخال المستخدم مباشرة مثل `./customers/{customer_id}`.
- **سيناريو هجوم:** تمرير `customer_id=../../../../tmp/pwn` قد يوجه التطبيق للقراءة/الكتابة خارج مجلد العملاء المتوقع.
- **الأثر:** كتابة/قراءة ملفات غير مصرح بها داخل صلاحيات العملية.
- **توصية:**
  - فرض whitelist صارمة على `customer_id` (مثلاً `^[a-zA-Z0-9_-]{1,64}$`).
  - استخدام `resolve()` والتحقق أن الناتج ضمن `customers` root.

### 3) Path Traversal عبر `file.filename` عند الحفظ (High)
- **الوصف:** اسم الملف المرفوع يستخدم مباشرة عند إنشاء مسار الحفظ المؤقت والنهائي.
- **سيناريو هجوم:** اسم ملف مثل `../../malicious.txt` قد ينتج عنه كتابة خارج المجلد المستهدف.
- **الأثر:** الكتابة في مسارات غير مقصودة + احتمال استبدال ملفات حساسة.
- **توصية:**
  - استخدام `Path(file.filename).name` فقط.
  - توليد اسم داخلي UUID بدل الثقة في الاسم الأصلي.

### 4) CORS غير آمن (Medium)
- **الوصف:** `allow_origins=["*"]` مع `allow_credentials=True`.
- **الأثر:** سلوك غير آمن/غير متوافق مع المتصفحات وقد يفتح سطح هجوم عبر Origins غير موثوقة.
- **توصية:** تقييد Origins لقائمة ثابتة بيئية (`ALLOWED_ORIGINS`).

### 5) تسريب تفاصيل الاستثناء للعميل (Medium)
- **الوصف:** في عدة مواضع يتم إرجاع `str(e)` مباشرة داخل `HTTPException` أو `details.exception`.
- **الأثر:** كشف معلومات داخلية (مسارات، stack hints، رسائل مكتبات خارجية).
- **توصية:**
  - إرجاع رسالة عامة للمستهلك.
  - تسجيل التفاصيل داخلياً فقط في logs.

### 6) غياب حدود حجم/عدد الملفات المرفوعة (High)
- **الوصف:** لا يوجد تحقق على `Content-Length` أو حجم الملف الفردي أو عدد الملفات في batch.
- **الأثر:** هجمات DoS عبر ملفات ضخمة أو batches كبيرة (استهلاك CPU/RAM/IO خاصة OCR/LLM).
- **توصية:**
  - حد أقصى للحجم (مثلاً 10–20MB/ملف).
  - حد أقصى للملفات في batch.
  - مهلات معالجة + queue + backpressure.

### 7) عدم تشديد التحقق من نوع الملف (Medium)
- **الوصف:** التحقق يعتمد على امتداد الملف فقط.
- **الأثر:** رفع ملفات بامتداد مزيف (polyglot/spoofed files).
- **توصية:** التحقق من MIME الفعلي + signature (magic bytes) قبل المعالجة.

### 8) مخاطر PII في السجلات (Medium)
- **الوصف:** يتم تسجيل `invoice_number` و`customer_id` ورسائل أخطاء قد تحتوي بيانات حساسة.
- **الأثر:** تسريب بيانات مالية/تعريفية إذا لم تؤمن logs جيداً.
- **توصية:**
  - سياسات masking/redaction.
  - تشفير logs at-rest.
  - تحديد retention ومبدأ أقل صلاحية.

---

## أخطاء تشغيلية محتملة (Reliability)
1. فشل OCR إذا لم تتوفر حزم لغات Tesseract المطلوبة (ara/eng/fra).
2. فشل الاستخراج إذا لم يتم ضبط `GROQ_API_KEY`.
3. التكوين الحالي يعتمد Mock config للعملاء (`create_agent_for_customer`) بدل تحميل إعدادات فعلية.

---

## خطة إصلاح مقترحة (بالأولوية)
1. **عاجل جداً:** AuthN/AuthZ + إغلاق Path Traversal (`customer_id` و`filename`).
2. **عاجل:** ضبط CORS ووقف تسريب الاستثناءات.
3. **عاجل:** حدود upload/rate limiting/timeouts.
4. **متوسط:** تحسين تدقيق نوع الملفات وhardening للـ logging.
5. **متوسط:** إضافة اختبارات أمنية (unit + integration) لحالات traversal ورفع ملفات كبيرة.

---

## ملاحظات
- هذه مراجعة أولية static review وليست penetration test كامل.
- يوصى بجولة ثانية تشمل DAST/SAST واعتماد checklist OWASP ASVS.
